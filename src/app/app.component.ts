import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // Selector del componente raíz
  templateUrl: './app.component.html', // Plantilla del componente
  styleUrls: ['./app.component.css'], // Estilos del componente
})
export class AppComponent {
  title = 'frontend'; // Puedes cambiar esto o eliminarlo
}